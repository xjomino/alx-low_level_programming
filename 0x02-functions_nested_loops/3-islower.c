#include "main.h"
/**
 * _islower - checks if it lowercase or not
 * @c: the character being checked
 *
 * Return: 1 if c is lowercase, if not 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
