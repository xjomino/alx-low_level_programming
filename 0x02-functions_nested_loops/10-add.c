#include "main.h"
/**
 * add - adds two integers and returns the result
 * @k: int to be added to b
 * @b: int to be added to k
 * Return: sum of k and b
 */
int add(int k, int b)
{
	int c;

	c = k + b;

	return (c);
}
