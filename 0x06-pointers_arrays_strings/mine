#include "main.h"
/**
 * reverse_array - reverse array of int
 * @a: the array
 * @n: upto the nth to be reversed)
 */
void reverse_array(int *a, int n)
{
	int k, b;

	for (k = 0; k < n / 2; k++)
	{
		b = a[k];
		a[k] = a[n - k - 1];
		a[n - k - 1] = b;
	}
}
